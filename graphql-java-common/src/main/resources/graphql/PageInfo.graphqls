
# 有关连接分页的信息
type PageInfo {
  # 当继续向上分页时,游标的开始位置.
  endCursor: String

  # 当继续向上分页，判断是否还有下一页数据?
  hasNextPage: Boolean!

  # 当继续向下分页, 判断是否还有上一页数据?
  hasPreviousPage: Boolean!

  # 当继续向下分页时, 游标的开始位置.
  startCursor: String

  totalCount:Int
}
